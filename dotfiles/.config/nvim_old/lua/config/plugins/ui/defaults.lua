return {
  {
    "echasnovski/mini.basics",
    version = "*",
    opts = {
      -- Options. Set to `false` to disable.
      options = {
        basic = true,
        extra_ui = false,
        -- Presets for window borders ('single', 'double', ...)
        win_borders = "default",
      },

      mappings = {
        basic = true,
      },

      autocommands = {
        basic = true,
      },
    },
  },
}
